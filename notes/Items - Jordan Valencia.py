class Item(object):
            def __init__(self, name, material):
                self.name = name
                self.material = material


class Sword(Item):
            def __init__(self, name, damage, decription, duration, length):
                self.name
                self.damge
                self.description
                self.duration
                self.length