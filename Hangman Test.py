import random
import string
print(list(string.ascii_letters))
letters_guessed = []
guess_left = 0
guess_total = 8
word_bank = ["Air"]
word_selected = random.choice(word_bank)
random.choice(word_bank)
for i in range(len(word_bank)):
    word_selected = random.choice(word_bank)

